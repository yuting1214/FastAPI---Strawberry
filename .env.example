# =============================================================================
# Development (default) - uses SQLite, no configuration needed
# =============================================================================
# Just run: uvicorn app.main:app --reload

# =============================================================================
# Production - requires DATABASE_URL
# =============================================================================
# Railway.com format (auto-injected when you add PostgreSQL service)
# DATABASE_URL=postgresql://postgres:PASSWORD@postgres.railway.internal:5432/railway

# Heroku format (auto-converted from postgres:// to postgresql://)
# DATABASE_URL=postgres://user:password@host:5432/dbname

# Standard PostgreSQL
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# =============================================================================
# Optional settings (have defaults)
# =============================================================================
# PORT=8000             # Server port (Railway auto-injects this)
# APP_MODE=dev          # Set to 'prod' to force production mode
# DB_POOL_SIZE=5        # Connection pool size (prod only)
# DB_MAX_OVERFLOW=10    # Max overflow connections (prod only)
